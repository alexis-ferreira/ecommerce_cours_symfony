{% extends 'base.html.twig' %}

{% block title %}

{% endblock %}

{% block body %}

    <table class="table table-hover">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Nom</th>
            <th scope="col">Prix</th>
            <th scope="col">Photo</th>
            <th scope="col">Action</th>
        </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <th>{{ article.id }}</th>
                <td>{{ article.nom }}</td>
                <td>{{ article.prix }} €</td>
                <td><img height="40" width="40" src="{{ asset('upload/') }}{{ article.photo }}" alt=""></td>
                <td>
                    <button type="button" class="btn btn-primary text-light text-decoration-none">Button</button>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

